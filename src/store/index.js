// import Vue from 'vue'
import  { createStore } from 'vuex'
import shortener from "./modules/shortener";
// Vue.use(Vuex)
export const store = createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    shortener
  }
})
